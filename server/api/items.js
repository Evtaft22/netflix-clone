const { Router } = require("express");
const db = require("../db");

const itemsRouter = Router();

// TODO: Your routes here!

// itemsRouter.get('/', (req, res) => {
// });

module.exports = {
  itemsRouter,
};
