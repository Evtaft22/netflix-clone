This is the boilerplate template for MVP

Must make a commit.
Then change the remote origin like this:
```
git remote add origin https://github.com/Evtaft22/[NEW-REPO].git
git push -u origin main
```
